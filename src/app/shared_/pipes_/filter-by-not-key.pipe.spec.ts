import { FilterByNotKeyPipe } from './filter-by-not-key.pipe';

describe('FilterByNotKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNotKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
