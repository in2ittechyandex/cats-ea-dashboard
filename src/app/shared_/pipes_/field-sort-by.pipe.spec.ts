import { FieldSortByPipe } from './field-sort-by.pipe';

describe('FieldSortByPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldSortByPipe();
    expect(pipe).toBeTruthy();
  });
});
