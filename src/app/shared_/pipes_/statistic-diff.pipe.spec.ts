import { StatisticDiffPipe } from './statistic-diff.pipe';

describe('StatisticDiffPipe', () => {
  it('create an instance', () => {
    const pipe = new StatisticDiffPipe();
    expect(pipe).toBeTruthy();
  });
});
